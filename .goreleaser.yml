version: 2

env:
  - GO111MODULE=on
  - GOPROXY="https://proxy.golang.org,direct"

archives:
  - id: default

builds:
  - binary: wtfutil
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm
      - arm64

before:
  hooks:
    - make install
